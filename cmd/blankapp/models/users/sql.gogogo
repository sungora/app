package users

// init Пользовательские зпапросы
func init() {
	sql.GetListFilter = `
	SELECT * FROM users WHERE updated_at < NOW() - INTERVAL 1 HOUR 
	`
}

type sqlTyp struct {
	GetListFilter string
}

// хранилище пользовательских запросов
var sql = new(sqlTyp)
